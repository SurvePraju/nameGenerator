<%- include("partials/header.ejs") %>
    <div>
        <% if (locals.adj && locals.noun) { %>
            <h1>
                <%= adj %>
                    <%= noun %>
            </h1>
            <% } else { %>
                <h1>Welcome to the Band Generator 🤟</h1>
                <% } %>



                    <form action="/generate-name" method="POST">
                        <button type="submit">Generate Name</button>
                    </form>

    </div>
    <%- include("partials/footer.ejs") %>
